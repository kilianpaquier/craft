# Code generated by craft; DO NOT EDIT.

name: Release
run-name: Release

<<- $nodejs := hasKey .Languages "nodejs" >>
<<- $hugo := hasKey .Languages "hugo" >>
<<- $golang := hasKey .Languages "golang" >>

<<- $gobuild := and $golang (gt (len .Clis) 0) (not .NoGoreleaser) >>
<<- $docker := and .Docker (gt .Binaries 0) >>

<<- $pages := and (.IsStatic "pages") (or $nodejs $hugo) >>
<<- $netlify := and (.IsStatic "netlify") (or $nodejs $hugo) >>

<<- $auth := fromPtr .CI.Auth.Release >>

on:
  workflow_dispatch:
    inputs:
      dry_run:
        description: Dry run
        type: boolean
        default: true
<<- if .CI.Release.Auto >>
  push:
    branches:
      - main
      - master
<<- if eq .CI.Release.Action "semantic-release" >>
      - v[0-9]+.x
      - v[0-9]+.[0-9]+.x
<<- end >>
<<- end >>

jobs:

  version:
    name: Version
    runs-on: ubuntu-latest
    environment: release
    outputs:
      release: ${{ steps.version.outputs.release }}
      version: ${{ steps.version.outputs.version }}
    steps:
      - uses: actions/checkout@v4
      - id: version
        uses: ./.github/actions/version
        with:
          dry_run: true # compute only the version to build
          token: ${{ secrets.GITHUB_TOKEN }}
<<- if and $nodejs (not (get .Languages "nodejs").Private) (eq .CI.Release.Action "semantic-release") >>
          npm_token: ${{ secrets.NPM_TOKEN }}
<<- end >>

<<- if or $gobuild $nodejs >>

  build:
    name: Build
    needs: version
    uses: ./.github/workflows/build.yml
<<- if $gobuild >>
    with:
      version: ${{ needs.version.outputs.version }}
<<- end >>
<<- end >>

<<- if $docker >>

  docker:
    name: Docker
    needs: version
    uses: ./.github/workflows/docker.yml
    with:
      version: ${{ steps.version.outputs.version }}
      release: ${{ steps.version.outputs.release }}
    secrets: inherit
<<- end >>

<<- if $netlify >>

  netlify:
    name: Netlify
    needs: build
    uses: ./.github/workflows/netlify.yml
    secrets: inherit
<<- end >>

<<- if $pages >>

  pages:
    name: Pages
    needs:
      - version
      - build
    uses: ./.github/workflows/pages.yml
    with:
      dry_run: ${{ inputs.dry_run }}
<<- end >>

  release:
    name: Release
    runs-on: ubuntu-latest
    if: ${{ github.ref_protected }}
    environment: release
<<- if or (eq $auth "github-token") $nodejs >>
    permissions:
<<- if eq $auth "github-token" >>
      contents: write
<<- if eq .CI.Release.Action "semantic-release" >>
      issues: write
      pull-requests: write
<<- end >>
<<- end >>
<<- if $nodejs >>
      id-token: write
<<- end >>
<<- end >>
    needs:
      - version
<<- if and (not $pages) (not $netlify) >>
      - build
<<- end >>
<<- if $docker >>
      - docker
<<- end >>
<<- if $netlify >>
      - netlify
<<- end >>
<<- if $pages >>
      - pages
<<- end >>
    steps:
<<- if eq $auth "github-app" >>
      - id: app_token
        uses: actions/create-github-app-token@v1
        with:
          app-id: ${{ vars.RELEASE_APP_ID }}
          private-key: ${{ secrets.PRIVATE_KEY }}
<<- end >>
      - uses: actions/checkout@v4
<<- if or (.CI.Release.Backmerge) (ne $auth "github-token") >>
        with:
<<- if .CI.Release.Backmerge >>
          fetch-depth: 0
<<- end >>
<<- if eq $auth "github-app" >>
          token: ${{ steps.app_token.outputs.token }}
<<- end >>
<<- if ne $auth "github-token" >>
          persist-credentials: false
<<- end >>
<<- end >>
      - uses: ./.github/actions/version
        with:
          download_dist: true
          dry_run: ${{ inputs.dry_run }}
<<- if eq $auth "github-app" >>
          token: ${{ steps.app_token.outputs.token }}
<<- else if eq $auth "personal-token" >>
          token: ${{ secrets.RELEASE_TOKEN }}
<<- else >>
          token: ${{ secrets.GITHUB_TOKEN }}
<<- end >>
<<- if and $nodejs (not (get .Languages "nodejs").Private) (eq .CI.Release.Action "semantic-release") >>
          npm_token: ${{ secrets.NPM_TOKEN }}
<<- end >>
<<- if and $nodejs (not (get .Languages "nodejs").Private) (ne .CI.Release.Action "semantic-release") >>
      # https://github.com/marketplace/actions/npm-publish
      - uses: JS-DevTools/npm-publish@v3
        with:
          dry-run: ${{ inputs.dry_run }}
          tag: ${{ needs.version.outputs.version }}
          token: ${{ secrets.NPM_TOKEN }}
<<- end >>