# Code generated by craft; DO NOT EDIT.

name: Pages
run-name: Pages

<<- $autoRelease := and .HasRelease .CI.Release.Auto >>

on:
  workflow_call:
  workflow_dispatch:
<<- /* avoid deploying two times when static and release are in auto mode */ ->>
<<- if and .CI.Static.Auto (not $autoRelease) >>
  push:
    branches:
      - main
      - master
      - v[0-9]+.x
      - v[0-9]+.[0-9]+.x
<<- end >>

jobs:

  build:
    name: Build
    uses: ./.github/workflows/build.yml

  pages:
    name: Pages
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    permissions:
      pages: write
      id-token: write
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
        with:
          artifact_name: github-pages
          preview: ${{ github.ref_name != github.event.repository.default_branch }}