# Code generated by craft; DO NOT EDIT.

name: Pages
run-name: Pages

on:
  workflow_call:
    inputs:
      dry_run:
        description: Dry run
        type: boolean
        default: false
  workflow_dispatch:
    inputs:
      dry_run:
        description: Dry run
        type: boolean
        default: false
<<- if .CI.Static.Auto >>
  push:
    branches:
      - main
      - master
<<- if not (.IsReleaseAction "gh-release") >>
      - v[0-9]+.x
      - v[0-9]+.[0-9]+.x
<<- end >>
<<- end >>

jobs:

  build:
    name: Build
    if: ${{ github.event_name == 'workflow_dispatch' }}
    uses: ./.github/workflows/build.yml

  pages:
    name: Pages
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    permissions:
      pages: write
      id-token: write
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
        with:
          artifact_name: github-pages
          preview: ${{ inputs.dry_run == 'true' || github.ref_name != github.event.repository.default_branch }}