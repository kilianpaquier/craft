# Code generated by craft; DO NOT EDIT.

name: Renovate
run-name: Renovate

on:
  workflow_dispatch:
    inputs:
      dry_run:
        description: Dry run
        type: boolean
        default: false
  schedule:
    - cron: "0 12 * * *"

jobs:
  renovate:
    name: Renovate
    runs-on: ubuntu-latest
    environment: renovate
    steps:
      # - id: app-token
      #   uses: actions/create-github-app-token@v1
      #   with:
      #     app-id: ${{ vars.RELEASE_APP_ID }}
      #     private-key: ${{ secrets.PRIVATE_KEY }}
      - uses: actions/checkout@v4
        # with:
        #   token: ${{ steps.app-token.outputs.token }}
      - uses: renovatebot/github-action@v40.2.5
        with:
          token: ${{ secrets.RENOVATE_TOKEN }}
          # token: ${{ steps.app-token.outputs.token }}
        env:
          RENOVATE_AUTODISCOVER: "true"
          RENOVATE_AUTODISCOVER_FILTER: kilianpaquier/craft
          RENOVATE_DRY_RUN: ${{ inputs.dry_run }}
          RENOVATE_GIT_AUTHOR: Renovate Bot <renovate-craft[bot]@users.noreply.github.com>