# Code generated by craft; DO NOT EDIT.

version: 2

builds:
  - main: cmd/name/main.go
    env:
      - CGO_ENABLED=0
    ldflags:
      - -X gitlab.com/kilianpaquier/craft/internal/build.date={{ .CommitDate }}
      - -X gitlab.com/kilianpaquier/craft/internal/build.branch={{ .Branch }}
      - -X gitlab.com/kilianpaquier/craft/internal/build.commit={{ .Commit }}
      - -X gitlab.com/kilianpaquier/craft/internal/build.version={{ .Env.VERSION }}
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64

announce:
  skip: true

changelog:
  disable: true

archives:
  - format: tar.gz
    wrap_in_directory: false
    name_template: >-
      {{- .ProjectName }}_
      {{- .Os }}_
      {{- .Arch }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
    format_overrides:
      - goos: windows
        format: zip

checksum:
  name_template: checksums.txt

nfpms:
  - maintainer: kilianpaquier
    file_name_template: >-
      {{- .ProjectName }}_
      {{- .Os }}_
      {{- .Arch }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
    formats:
      - apk
      - deb
      - rpm