# Code generated by craft; DO NOT EDIT.

name: Release
run-name: Release

on:
  workflow_dispatch:
    inputs:
      mode:
        description: Whether to only make a dry_run, create release / pull request as draft or to make the release completely
        type: choice
        default: dry_run
        options:
          - dry_run
          - draft
          - release

jobs:

  version:
    name: Version
    runs-on: ubuntu-latest
    if: ${{ github.ref_protected }} # skip all jobs since they depend on version job
    environment: release
    permissions:
      contents: write
      id-token: write
    outputs:
      release: ${{ steps.version.outputs.release }}
      version: ${{ steps.version.outputs.version }}
    steps:
      - uses: actions/checkout@v4
      - id: version
        uses: ./.github/actions/version
        with:
          mode: dry_run # compute only the version to build
          token: ${{ secrets.GITHUB_TOKEN }}

  build:
    name: Build
    needs: version
    uses: ./.github/workflows/build.yml

  release:
    name: Release
    runs-on: ubuntu-latest
    environment: release
    permissions:
      contents: write
      id-token: write
    needs:
      - version
      - build
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/version
        with:
          download_dist: true
          mode: ${{ inputs.mode }}
          token: ${{ secrets.GITHUB_TOKEN }}
      # https://github.com/marketplace/actions/npm-publish
      - uses: JS-DevTools/npm-publish@v3
        with:
          dry-run: ${{ inputs.mode != 'release' }}
          tag: ${{ needs.version.outputs.version }}
          token: ${{ secrets.NPM_TOKEN }}